/**
 * Common Images
 * 
 * All common/shared images for the application.
 * This includes frequently used images that don't fit into specific categories.
 */

// Common shared images that are used across multiple components
// Add images here that are used in multiple contexts or don't fit
// into other specific categories

// Placeholder for common images
// These will be populated as we identify commonly used images

// Default export for convenience
export default {
  // Common images will be added here
};

/**
 * Common image guidelines:
 * 
 * - Use for images that are shared across multiple components
 * - Avoid duplicating images that belong in specific categories
 * - Consider creating subcategories if this grows large
 * - Use descriptive names that indicate usage context
 * - Ensure images are optimized for reuse
 */
